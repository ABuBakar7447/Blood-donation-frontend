git push origin --delete main
** copy url
** in CMD write " git clone 'paste the link you copied"
*** after install write "npm i"
**** then write, "code ."
***** then write, "npm run dev"
** then click the link, "ctrl + click the link"